import { PageHeader } from "@/components/data/page-header";

/** Gestión de Usuarios — usuarios, perfiles y roles (placeholder; solo habilita la ruta). */
export default function UsuariosPage() {
  return (
    <PageHeader
      title="Gestión de Usuarios — Usuarios, perfiles y roles"
      description="Administración de usuarios, perfiles institucionales y roles."
    />
  );
}
